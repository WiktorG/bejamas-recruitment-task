const initialState = {
  isVisible: false,
  items: [],
}

function cartReducer(state = initialState, action) {
  switch (action.type) {
    default:
      return state
  }
}

export default cartReducer
